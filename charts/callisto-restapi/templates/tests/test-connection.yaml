apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "callisto-restapi.fullname" . }}-test-connection"
  labels:
    {{- include "callisto-restapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "callisto-restapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
