apiVersion: v1
kind: Service
metadata:
  name: {{ include "callisto-restapi.fullname" . }}
  labels:
    {{- include "callisto-restapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "callisto-restapi.selectorLabels" . | nindent 4 }}
